## 0. Spikes and prerequisites

Phases 1–4 leave the live travel site visually identical; nothing below is depended on until 0.1 and 0.2 answer.

- [x] 0.1 **Spike the CSS alias.** Prove that `@import "#brand-theme"` inside `frontend/src/index.css` resolves through `resolve.alias` under `@tailwindcss/vite`, in **both** `vite dev` (with HMR on edit) and `vite build`. Code-reading says `@tailwindcss/vite` builds its `customCssResolver` from Vite's own resolver, but the whole CSS plan rests on this. Throw the spike away. If it fails, fall back to the ~15-line `transform(code, id)` plugin described in design.md and record the fallback in the commit message. — **PASSED, no fallback needed.** A `#spike-theme` `resolve.alias` pointing at a throwaway `src/spike-theme.css` resolved through `@import "#spike-theme"` inside `src/index.css` in both modes: `vite build` emitted the sentinel token into `dist/assets/index-*.css`, and `vite dev` served it from `/src/index.css?direct`. HMR confirmed live over the `vite-hmr` websocket — editing the aliased file pushed `{"type":"css-update","path":"/src/index.css?direct"}` and a refetch showed the new value. The `transform(code, id)` fallback in design.md is **not** required. Spike reverted.
- [x] 0.2 **Spike dead-code elimination.** Prove that a `define`d `import.meta.env.VITE_BRAND` literal plus a ternary in `src/brand/index.ts` drops the unselected brand module: build with a throwaway second brand containing a unique sentinel string, then `grep dist/assets/*.js` for it. Confirm that a bare `import.meta.env.VITE_BRAND` with the variable **unset** does *not* get substituted — this is why `define` is required and not a stylistic preference. — **Ternary confirmed; one design.md sub-claim did not reproduce.** With two throwaway brand modules carrying unique sentinels: the **ternary** shape ships exactly one (`VITE_BRAND=alpha` → ALPHA only; `=bravo` → BRAVO only), and the **`Record` lookup** shape ships **both** — the decision to use a ternary is load-bearing exactly as stated. However, design.md's claim that a *bare, unset* `import.meta.env.VITE_BRAND` "leaves a runtime property access — and both brands ship" **does not hold on this toolchain** (Vite 8 / rolldown): unset, it folds to `undefined`, takes the fallback arm, and still drops the other brand. `define` is therefore **not** what makes DCE work here — but it stays, because it is what makes the build *fail loudly* on an unset or unrecognized value rather than silently shipping the fallback brand, and because relying on rolldown's folding of an undefined property is a silent-regression risk. Keep task 1.5 as written; the reason in the code comment should be fail-loud, not DCE. Spike reverted.
- [x] 0.3 **Done — verified live.** `officelingobingo.com` is registered and delegated (`ns-1368`/`ns-1924`/`ns-37`/`ns-564` .awsdns), and exactly **one** hosted zone exists (`Z0218183N77UW58I2VEO`), so the duplicate-zone hazard did not materialise. Note the original hand-made zone `Z07251713HNFEPFEO5THH` is gone — registration produced a new zone and the old one was cleaned up. **PARTIALLY DONE — the hosted zone exists but the domain is NOT registered.** Route53 hosted zone `Z07251713HNFEPFEO5THH` for `officelingobingo.com.` is created and Terraform has written the ACM validation CNAME into it, but Verisign returns "No match for domain" and the `.com` registry returns NXDOMAIN — creating a hosted zone does not reserve the name. This is what failed the 5.9 dev apply: `aws_acm_certificate_validation` timed out after 1h15m in PENDING_VALIDATION, because the validation record lives in a zone nothing delegates to. Remaining: register the domain, then point its nameservers at this zone's four: `ns-661.awsdns-18.net`, `ns-16.awsdns-02.com`, `ns-1173.awsdns-18.org`, `ns-1805.awsdns-33.co.uk`. Confirm with `dig +short NS officelingobingo.com` before re-running the apply.
- [x] 0.4 **Done in the Google console by the repo owner.** **Reuse the existing Google Cloud project** — do *not* create a second one. Two things to check. **(a) Authorized domains.** The list holds the two *Cognito* hostnames (`travelbingo-{dev,prod}.auth.us-east-1.amazoncognito.com`), not a registrable domain and not the app's domain — Google never sees the app's domain, because the redirect URI points at Cognito's hosted UI. So add the office pair in the same form: `officelingobingo-dev.auth.us-east-1.amazoncognito.com` and `officelingobingo-prod.auth.us-east-1.amazoncognito.com`. (Confirm first whether that list is the consent screen's *Authorized domains* or an OAuth client's *Authorized redirect URIs* — if the latter, it is per-client and task 5.7 already covers it. Add the office pair wherever the travel pair appears.) Both hostnames assume `cognito_domain_prefix` is left empty so it defaults to `bucket_name`. **(b) Publishing status.** The screen must be **published**, not "Testing", which caps sign-in to explicitly listed users (100 max). Travel prod already serves arbitrary users, so this is almost certainly already true — confirm rather than change. **Decision, recorded deliberately:** a Google Cloud project has exactly one consent screen, so office visitors will be asked to grant access to **"Travel Bingo"**, with that app name, logo, and privacy links. The original plan was a separate project to avoid exactly this; the repo owner judged the mismatch acceptable for a satire site. This does **not** affect account separation — see design.md's risk entry and `infra/README.md`.
- [x] 0.5 **Checked: it DOES set `environment = "prod"` explicitly — so production deletion protection has been ON all along, and the latent bug did not bite.** The corrected default and the new `contains(["dev","prod"])` validation are therefore a guard against future drift rather than a fix for a live exposure, and the prod workspace will pass validation unchanged. Check whether the live `travelbingo-prod` HCP workspace sets `environment = "prod"` explicitly. If it does not, DynamoDB deletion protection is currently **off in production** — record the finding; task 5.4 fixes it.
- [x] 0.6 **Skipped by decision** — not verified. Cognito user-pool pricing remains the one unquantified cost line; at hobby MAU it is expected to round to zero, but that is an assumption, not a check. Verify Cognito's current user-pool pricing tier and free-MAU allowance for a pool created today in `us-east-1`. It is the only cost line in this change that could surprise.
- [x] 0.7 Let `add-trips` finish and commit. — **Confirmed: landed in `85e57a7`, `21ec600`, `8c29676`.** Its four trip pages carry ~150 `trip` references, of which the user-facing subset (roughly 40 distinct strings) is what phase 3 retrofits; the rest are identifiers, types, and route paths that stay `trip` in every brand.

## 1. The brand seam, travel only — no visual change

- [x] 1.1 Create `frontend/src/base.css` and move into it everything brand-independent from `index.css`: `@custom-variant dark`, the whole `@theme inline` bridge, the `html {}` base paint, `.emoji-picker-themed`, the `prefers-reduced-motion` kill switch, and the prose comments that make those decisions readable. Leave `index.css` as **imports only** — `@import "tailwindcss"`, `#brand-theme`, `#brand-motifs`, `./base.css`, in that order. CSS requires `@import` to precede all other rules, so a `:root` block left in `index.css` would sit after the brand import and win; that is why this file ends up empty of rules.
- [x] 1.2 Create `frontend/src/brand/travel/theme.css` with the `:root` and `[data-theme="dark"]` token blocks, `--radius`, and `--font-display`. Move the `@fontsource-variable/outfit` import out of `main.tsx` and into this file — it is a travel-brand asset, and the office brand will download no chrome font at all.
- [x] 1.3 Create `frontend/src/brand/travel/motifs.css` with the two `@utility` definitions, renamed by role: `edge-perf` → `panel-edge`, `bg-map-grid` → `bg-page-texture`. Carry the existing comments across, including the one-motif-per-surface warning on the perforated edge.
- [x] 1.4 Rename `--shadow-postcard` → `--shadow-raised` and `--ocean`/`--color-ocean` → `--brand-accent` across `theme.css`, `base.css`, and every consumer: `AppShell.tsx`, `SiteHeader.tsx`, `CardView.tsx`, `Panel.tsx`, `SuggestionsDialog.tsx`, `ui/chip.tsx`, `SavedCardsPage.tsx`, `SharedCardPage.tsx`, `dev/gallery/designSamples.tsx`. `--paper` and `--stamp` are already role-named and stay. **Do not touch** `src/lib/colorScheme.ts` or `suggestedThemes.json` — they match a grep for "ocean"/"paper" but those are card content (a theme *named* ocean), not tokens.
- [x] 1.5 Add `vite.config.ts` brand plumbing: the `BRAND_IDS` closed list, `VITE_BRAND` validation that **throws on `vite build`** and defaults to `travel` on `vite dev`, `define: { "import.meta.env.VITE_BRAND": JSON.stringify(brandId) }`, and the `#brand-theme` / `#brand-motifs` aliases. Required-on-build is what stops a misconfigured CI job shipping travel assets to the office bucket; defaulted-on-dev is what preserves "a fresh clone with no `.env.local` still works". — **Note:** `BRAND_IDS` is `['travel']` for now; task 4.4 adds `office`. `brand.contract.test.ts` asserts the list agrees with `src/brand/` and the registry, so the three cannot drift.
- [x] 1.6 Create `frontend/src/brand/types.ts` (`BrandId`, `Brand`, `BrandCopy`, `BrandMeta`) and `frontend/src/brand/travel/index.ts`. `Brand` carries `id`, `name`, `storagePrefix`, `MarkIcon`, `copy`, `suggestions`, `meta`. — **Deviation:** `Brand` carries `id`, `name`, `storagePrefix`, `MarkIcon`, `suggestions` only. `copy` and `BrandCopy` arrive with task 3.1 and `meta`/`BrandMeta` with task 2.1, because their *values* are the deliverable of those phases — adding the fields now would mean either fabricating values or shipping dead data, and would break the property that each phase builds and reverts on its own.
- [x] 1.7 Create `frontend/src/brand/index.ts` selecting with a **ternary**, not a `Record` lookup — an object literal referencing both arms defeats dead-code elimination. Create `frontend/src/brand/registry.ts` holding `{ travel } satisfies Record<BrandId, Brand>`, imported **only by tests** so it never enters the production graph. — **Note:** with one brand the selection is a constant, not yet a ternary; the ternary shape and the reason for it are documented in place, and 4.4 makes it one. The reachability guard reads the file as text, so it covers both forms.
- [x] 1.8 Move `src/data/suggestedCells.json` and `suggestedThemes.json` to `src/brand/travel/`; point `src/lib/suggestions.ts` at `brand.suggestions` instead of its two static imports. The normalizer and `appendCells` are untouched — the JSON is the only thing that moves.
- [x] 1.9 Create `frontend/src/brand/tokens.contract.test.ts`: read `base.css` as text, extract every `var(--x)` referenced by the `@theme inline` bridge, and assert each is declared by every brand in **both** `:root` and `[data-theme="dark"]`; assert every brand's `motifs.css` defines the full slot list; assert neither `index.css` nor `base.css` declares a brand-owned custom property (the `npx shadcn add` drift guard). This is the highest-value new test — a token missing from one brand renders as *nothing*, with no error anywhere.
- [x] 1.10 Create `frontend/src/brand/brand.contract.test.ts`: registry exhaustive over `BrandId`; `index.ts`'s ternary covers every registry key (source-read, in the style of `dev/gallery/coverage.test.ts`); `copy` key sets match recursively across brands; `storagePrefix` values unique and non-empty; each brand's suggestion data normalizes to ≥1 category and ≥1 theme. That last assertion also closes an existing gap — `normalizeCategory`/`normalizeTheme` have no test today, so malformed JSON degrades silently to an empty dialog.
- [x] 1.11 Extend `frontend/scripts/check-bundle.mjs` to assert the built bundle contains the selected brand's `name` and **not** any other brand's. It already proves the dev gallery was eliminated; this is the same mechanism applied to the same risk.
- [x] 1.12 Add one assertion to `frontend/src/components/cardGrid.guard.test.ts`: `CardGrid.tsx` must not contain `@/brand` or `brand.`. One line, closing the single biggest new risk to the frozen renderer — someone reaching for `brand.copy` inside the card.
- [x] 1.13 Add `VITE_BRAND: travel` to the `dev` and `prod` GitHub Environment `vars`, and `VITE_BRAND: ${{ vars.VITE_BRAND }}` to the build step's `env` in `.github/workflows/_deploy.yml`. — **Workflow side done; the GitHub Environment `vars` are a console action still outstanding.** `_deploy.yml` now passes `VITE_BRAND: ${{ vars.VITE_BRAND }}`, so **`VITE_BRAND=travel` must be set on the `dev` and `prod` GitHub Environments before the next deploy** — without it `vite build` fails the deploy loudly (by design) rather than shipping the wrong assets.
- [x] 1.14 **Definition of done for this phase: no visual change. CONFIRMED.** `npm run capture -- /` and `-- /ui` byte-identical against copies taken before the change, plus the full export regression checklist from `DESIGN.md` (print Letter and A4, PNG, thumbnail) — `--shadow-raised` touches `CardView`, so the card's surroundings changed even though the card did not. — **Result: production output is pixel-identical.** All 8 screenshots (`/` and `/ui`, light and dark, 390 and 1440) compare **0 differing pixels** against a baseline built from `HEAD`; both print PDFs (Letter and A4) differ in exactly **4 bytes**, all inside the `/CreationDate` and `/ModDate` timestamps. Two traps worth recording for the phase-4 and phase-7 capture runs: the baseline must be given the untracked `frontend/.env.local`, or it builds with accounts disabled and the whole header nav differs; and `/ui` is dev-only, so against a `vite preview` build it silently renders the app's catch-all route instead of the gallery. **One genuine change, and it is an improvement:** on the *dev server* the chrome headings previously rendered in a fallback face — Outfit was pulled in by a JS-side import in `main.tsx` and arrived after first paint — so the dev server did not match its own production build. Now that the font is imported by `brand/travel/theme.css` it loads with the stylesheet, and dev matches production exactly. The visual review loop in DESIGN.md runs against the dev server, which means it was previously being reviewed in the wrong typeface.

## 2. Marketing surface, travel only

- [x] 2.1 Create `frontend/src/brand/travel/meta.json`: `name`, `title`, `description`, `themeColorLight`, `themeColorDark`, `markHex`, `markFgHex`, `faviconPath`. **JSON, not TS** — `vite.config.ts` runs in Node and must read the same data the app reads, and `brand/<id>/index.ts` imports `lucide-react`, which would drag React into config load. — **Note:** `themeColorLight`/`themeColorDark` are the sRGB resolution of `--background` in each presentation, computed from the oklch rather than eyeballed (`#faf6ee` / `#151b24`). The same conversion reproduces the favicon's existing `#c74726` / `#fefbf6` exactly, which is the check that the arithmetic is right.
- [x] 2.2 Add a local `brandHtml()` `transformIndexHtml` plugin to `vite.config.ts`: replace `<title>`, rewrite the icon `href`, and inject `description`, `og:title`/`og:description`/`og:type`/`og:url`, `twitter:card`/`twitter:title`/`twitter:description`, `<link rel="canonical">`, and two `theme-color` metas (one under `media="(prefers-color-scheme: dark)"`). `og:url` and `canonical` come from the existing `VITE_APP_ORIGIN`; **omit both when it is unset** rather than emitting an unresolvable URL. Omit `og:image` entirely until a preview image exists. — **Note:** `index.html`'s title and icon are now explicit placeholders with a comment saying so, since editing them has no effect on any build.
- [x] 2.3 Rename `public/favicon.svg` → `public/favicon-travel.svg`.
- [x] 2.4 Create `frontend/src/brand/meta.contract.test.ts`: each brand's favicon file exists and contains both `markHex` and `markFgHex`; `title` and `description` non-empty; description under ~155 characters. This converts the existing hand-sync comment in the favicon into a test — it cannot verify the hex matches the oklch without a colour library, but it catches the palette moving without the favicon following, which is the failure that actually happens.
- [x] 2.5 Extend `check-bundle.mjs` to read `dist/index.html` and assert its title, description, and favicon path match the selected brand's `meta.json`.
- [x] 2.6 **Verified.** Verify: the travel site gains a description, OG, Twitter, canonical, and theme-color tags it does not have today. Confirm the rendered `<head>` in `dist/index.html` and that the favicon still resolves. — **Result:** the travel site now serves a description, `og:type`/`og:title`/`og:description`/`og:site_name`, `twitter:card`/`twitter:title`/`twitter:description`, `canonical`, and two `theme-color` tags it did not have before, plus a brand title and `/favicon-travel.svg` (confirmed present in `dist/` and referenced from `dist/index.html`). With `VITE_APP_ORIGIN` unset, `og:url` and `canonical` are **absent** rather than unresolvable, as required. Both new guards were confirmed to bite: reordering the attributes on `index.html`'s icon link so the plugin's regex misses fails the build with `dist/index.html has no icon link`.

## 3. Copy extraction, travel only

- [x] 3.1 Define `BrandCopy` in `brand/types.ts` and populate `brand/travel/copy.ts`. Extract the ~25 varying strings: the wordmark in `SiteHeader.tsx`, the nav labels, `CardDetailsForm.tsx`'s title placeholder, `SharedCardPage.tsx`'s fallback card name, and `imageExport.ts`'s `DEFAULT_IMAGE_FILENAME`. Capitalized variants are **explicit keys**, not a `capitalize()` helper. — **Note:** `BrandCopy` landed at 20 keys, close to design.md's ~25 target, because most trip sentences differ *only* in the noun and are composed at the call site from `copy.noun.{trip,trips,Trip,Trips}` — the four capitalized forms exist exactly so no `capitalize()` helper has to guess. Explicit keys are reserved for sentences whose structure a second brand would rephrase (`signedOutPitch`, `emptyState`, `modeHint`, `deleteWarning`, `removeMemberWarning`, `endedNotice`, `noLongerAvailable`, `notFoundOrNotMember`, `inviteInvalidReason`). One key is a **function** — `winConditionUnreachable(count)` — because it is the only string that pluralizes, and a brand that pluralizes its noun differently should own that in its own file rather than have a count spliced in at the call site.
- [x] 3.2 Retrofit the Trips UI: move the ~40 user-facing "trip" occurrences in `TripsPage.tsx`, `TripFormPage.tsx`, `TripDetailPage.tsx`, `InvitePage.tsx`, and the `SiteHeader` nav entry to `brand.copy.trips.*` / `brand.copy.noun.*`, and make the `Compass` icon brand-selected. Component names, file names, `tripApi.ts`, `tripTypes.ts`, and the `TRIP#` key prefix all stay `trip` in every brand. — **Deviation: a second icon field was needed.** The task says to make "the `Compass` icon brand-selected", but `Compass` is not the wordmark mark — it stands for a *trip* on the list tiles, the details panel, the cards panel, and the invite page, in four files. Reusing `MarkIcon` for it would have changed the travel site's pixels (Compass → MapPin), so `Brand` gained a separate `TripIcon`. `MarkIcon` is the product; `TripIcon` is one object inside it, and a brand whose noun is "meeting" wants a calendar there rather than its own wordmark repeated.
- [x] 3.3 Create `frontend/src/lib/routes.ts` (`ROUTES = { editor, cards, trips, settings, … } as const`) and use it at every `NavLink to` / `navigate()` / `signIn()` / `<Navigate to>` site. Paths stay brand-invariant — invite and share links are persisted capability URLs — but this makes the decision reversible in one file rather than twenty call sites. — **Note:** `ROUTES.trip(tripId)` surfaced a latent nullability the old template literal hid — `useParams` gives `string | undefined`, and `/trips/${undefined}` would have been a live path. Both call sites were already guarded by `isEdit`/`notAdmin` and now use the file's existing `tripId!` convention. The `<Route path=…>` literals in `routes.tsx` are deliberately **not** built from `ROUTES`: a route table that reads from the constants it defines cannot disagree with itself, and that disagreement is the one worth catching.
- [x] 3.4 Route `authSession.ts` and `colorMode.ts` storage keys through `brand.storagePrefix`. Not urgent (the two sites are different origins, so localStorage is already isolated) but it removes a confusing hardcoded string for three lines.
- [x] 3.5 Add to `AGENTS.md` the rule that keeps `BrandCopy` from metastasizing: **a string enters it only when the two brands' values actually differ.** `Save`, `Cancel`, `PDF`, `PNG`, `Entries`, `Look & feel`, `Card details` are identical in both brands and stay inline. — **Done, plus more than the one rule.** AGENTS.md also gained the brand-seam invariants (exactly one brand per build, ternary-not-Record, registry is test-only), the brand-invariance rule for the card renderer / stored shape / API / URL paths, the role-not-picture token naming rule, and the `npx shadcn add` follow-up. Stale references were corrected throughout — `src/index.css` is no longer the token layer, and `--ocean` is now `--brand-accent`. Several code comments pointing at `index.css` for the dark variant and the base page paint were corrected to `base.css` in the same pass.
- [x] 3.6 **Verified.** Verify: `npm run capture` still identical to the pre-change copies. This phase moves strings; it must not move pixels. — **Result: 12 of 12 screenshots pixel-identical** against the `HEAD` baseline — `/`, `/trips`, and `/cards`, each light and dark at 390 and 1440. `/trips` and `/cards` were added to the matrix because `/` alone does not exercise a single string this phase moved. **The print PDFs differ, and the difference is understood and intended:** holding `meta.json`'s title constant at `"Travel Bingo"` and rebuilding brings them to **8 differing bytes, all inside `/CreationDate` and `/ModDate`** — so the entire remaining difference is the `/Title` metadata that phase 2 deliberately changed. The printed page content is byte-identical. **Worth knowing:** `capture.mjs` calls `Page.printToPDF` without `displayHeaderFooter`, so the title reaches only the PDF's metadata here — but a real user printing from the browser dialog *does* get the document title in the page header by default, and that header line is now the longer marketing title rather than "Travel Bingo". That is a consequence of giving the page a search-worthy title, which `meta.contract.test.ts` requires to differ from the bare product name; flagging it as a real if minor user-visible change to printed output.

## 4. The office brand — frontend only, nothing deployed

- [x] 4.1 `brand/office/theme.css`: cool printer-paper off-white page (not warm cream), flat corporate blue `--primary`, highlighter yellow-green `--brand-accent`, bureaucratic red `--stamp`, cool slate dark mode (not warm navy), squarer `--radius`, and `--font-display` set to the system stack so **no chrome font is downloaded** — cheaper, and exactly the joke.
- [x] 4.2 `brand/office/motifs.css`: `bg-page-texture` as spreadsheet cell ruling (the same 64px grid SVG with the tick paths deleted); `panel-edge` as a three-hole-punch binder edge (same `radial-gradient` mask technique, three holes down the left, `mask-repeat: no-repeat`).
- [x] 4.3 **Checked. Hazard was real; resolved by moving `--stamp` to violet.** A blue `--primary` retires the terracotta/crimson confusion `DESIGN.md` warns about and creates a new one: red `--stamp` against `--destructive`. The token guard checks presence, never value, so this is a human check — both presentations, and the chip and alert surfaces specifically. — **Measured, not eyeballed: a bureaucratic red `--stamp` against this brand's red `--destructive` came out at 1.18:1.** Indistinguishable, exactly as predicted. `--stamp` is therefore **violet** — `oklch(0.45 0.14 310)` light, `oklch(0.74 0.13 312)` dark — 282deg of hue from `--destructive` and 7.43:1 on the page, so it still carries the chip's 12px lettering. This is a **deliberate departure from design.md's "bureaucratic red"**, and it is also the authentic answer: aniline stamp pads are why every RECEIVED and every date stamp in an office is purple. Confirmed visually on the `/ui` token strip in both presentations, not just numerically. **A second hazard surfaced that nothing had predicted:** a corporate-blue `--primary` sat 5deg from a blue `--info`, so `--info` was moved to teal (43deg separation) — an info alert must not read as a primary action.
- [x] 4.4 `brand/office/copy.ts` and `meta.json`; `public/favicon-office.svg` with the `TrendingUp` mark; `MarkIcon: TrendingUp` in `brand/office/index.ts`. Add `office` to `BRAND_IDS`, to the registry, and to the ternary in `brand/index.ts` — the guards from 1.9/1.10 now have a second brand to compare against and start earning their keep. — **Note:** `TripIcon` is `CalendarDays`, separate from `MarkIcon` (`TrendingUp`) — see the 3.2 note for why the two are different fields.
- [x] 4.5 `brand/office/suggestedCells.json`: roughly four categories × 15 cells. Proposed categories — Standup, All-Hands, Client Call, Performance Review. Proposed cells — "You're on mute", "Let's take that offline", "Circle back", "Can everyone see my screen?", "Let's parking-lot that", "Low-hanging fruit", "Double-click on that", "Do you have bandwidth?", "Move the needle", "Boil the ocean", "Table stakes", "North star", "Deep dive", "Socialize this", "Ping me". First draft; expected to be revised in 4.8. — **Note:** 4 categories x 15 cells. Standup, All-Hands, Client Call, Performance Review, as proposed.
- [x] 4.6 `brand/office/suggestedThemes.json`: Slide Deck, Spreadsheet, Legal Pad, Whiteboard, Redline, Q4 Earnings. These carry raw hex, which is correct — they are card content, not app chrome. **Every font must come from the shared `ALLOWED_FONTS` allowlist**, which `cardPayload.ts` duplicates byte-for-byte from `fontScheme.ts`; a theme naming anything else makes the card unsaveable with a 400 far from the cause. — **Note:** 6 themes; every font checked against `FONT_OPTIONS` by the new guard in 4.7. `Whiteboard` uses Comic Sans deliberately.
- [x] 4.7 Extend `brand.contract.test.ts` with the font check: every font in every brand's `suggestedThemes.json` is in `ALLOWED_FONTS`.
- [x] 4.8 Add `VITE_BRAND` to `frontend/scripts/capture.mjs`'s output slug. Without it, capturing `/` in the office brand **silently overwrites** the travel captures — a genuinely confusing review failure. Then review `/`, `/ui`, `/cards`, and `/trips` in both brands, light and dark, 390px and 1440px, and iterate on the office palette and content. **This is where the design is judged, with zero infrastructure committed and zero cost incurred.** — **Slug done; review done; one finding.** `capture.mjs` now prefixes every output with the brand (`office-root-1440-light.png`), defaulting to `travel` to match the dev server's own default. Both brands reviewed at `/` and `/ui`, light and dark, 390 and 1440. **The office brand lands:** the deadpan chrome against "You're on mute" is where the satire actually works, and both CSS motifs generalized without `AppShell` or `CardView` changing a line — the strongest evidence the slots were named for the right thing. **One thing for your eye:** `AppShell`'s page wash (`from-primary/[0.06] via-transparent to-brand-accent/[0.05]` plus a `bg-brand-accent/15` blur blot) is livelier in office than in travel, because a saturated blue and a highlighter green tint more than terracotta and teal do. It reads as a faint blue-to-green gradient across the page, which is slightly at odds with "deliberately boring". The opacities live in the shared `AppShell`, so lowering them would change travel too; the in-seam fix is to drop office's `--brand-accent` chroma. Left as-is pending your judgement.
- [x] 4.9 Write `frontend/src/brand/travel/BRAND.md` and `office/BRAND.md` with each brand's palette, typeface, and motif realizations. Rewrite `frontend/DESIGN.md` to hold the **rules** only — token roles, motif slots, the one-motif-per-surface constraint, spacing, type, depth, focus, the frozen renderer, the export checklist — pointing at the BRAND.md files for specifics. Do not turn DESIGN.md into two columns; its value is that it is specific, and parameterizing it would make both halves mushy. — **Note:** DESIGN.md's colour table became a *role* table (what each token is for, not what colour it is) and the motif inventory became the slot list; the review loop gained the `VITE_BRAND` invocations and the per-brand review rule. Palettes, typefaces, and motif realizations moved to `src/brand/{travel,office}/BRAND.md`.
- [x] 4.10 **Done early, in phase 1.** Land `.github/workflows/ci.yml` from `openspec/changes/add-branch-protection/`, with a build matrix over both `VITE_BRAND` values. **This is a prerequisite, not polish**: lint/test/build currently run only on the deploy path, and a build exercises only the selected brand's CSS graph — so without it, nothing would ever build the office brand except a production deploy. — **Landed in phase 1 out of necessity:** `ci.yml` had already shipped with `add-branch-protection`, and its `npm run build` broke the moment `VITE_BRAND` became required. It builds **every** brand, via a loop over `src/brand/*` rather than a `strategy: matrix` — the brand list then comes off disk and cannot drift, and the job keeps the single check name `frontend`, where a matrix would rename it to `frontend (travel)` and force a branch-protection edit with every brand added.

## 5. Infrastructure

- [x] 5.1 `infra/bootstrap`: replace the four scalar `dev_*`/`prod_*` variables with one `variable "environments"` of type `map(object({ bucket, workspace, role_name_prefix, github_environment }))`, and turn `local.envs` into a comprehension over it in `tfc-roles.tf`, `gha-roles.tf`, and `lambda-roles.tf`.
- [x] 5.2 Keep the existing map keys **`dev` and `prod`** and set their `role_name_prefix` to `travelbingo`, so `terraform plan` shows additions only. `for_each` keys are part of the resource address (`aws_iam_role.tfc["dev"]`), and the existing role ARNs are pasted into HCP and GitHub configuration — a destroy/recreate breaks the running deploys. New keys: `office-dev`, `office-prod`, prefix `olbingo`. — **Verified offline against the live state file.** All six existing roles keep both their address and their name — `aws_iam_role.{tfc,gha,lambda}["dev"|"prod"]` → `travelbingo-{tfc,gha,lambda}-{dev,prod}` — and the six office roles are pure additions (`olbingo-{tfc,gha,lambda}-office-{dev,prod}`). Nothing is renamed or dropped, so a plan should be additions only; **still confirm that on the real plan before applying** (5.6).
- [x] 5.3 Make `github_environment` an explicit field rather than reusing `each.key`. Today `gha-roles.tf` builds the OIDC subject from `each.key` while `tfc-roles.tf` builds role names from `role_suffix`; they are equal **by coincidence**, and a new entry where they differ would silently produce a role nothing can assume. Update the `table_name`/`function_name` comment to say that `bucket_name` is the brand-and-environment discriminator.
- [x] 5.4 **Code done; the 0.5 finding is still outstanding.** `infra/variables.tf`: add `variable "brand" { default = "travelbingo" }`; change `environment`'s default from `"production"` to `"prod"` and add `validation { contains(["dev","prod"], var.environment) }`; correct its description, which says "used for tagging" when it is actually a behaviour switch gating the Cognito admin-auth flow, the localhost callback URL, and DynamoDB deletion protection. Act on the 0.5 finding. — `var.brand` added (tag only), `environment` default corrected `"production"` → `"prod"`, validation `contains(["dev","prod"])` added, and the description rewritten to say it is a behaviour switch rather than a tag. **What I could not do: check whether the live `travelbingo-prod` HCP workspace sets `environment` explicitly (task 0.5).** If it does not, it has been running on the old `"production"` default, which matches no comparison in the config — so `dynamodb.tf`'s `== "prod"` gate has been false and **production deletion protection is currently OFF**. The validation added here is what surfaces that on the next plan: the workspace will fail validation until the variable is set, and setting it to `prod` turns deletion protection on. Expect that diff and do not dismiss it.
- [x] 5.5 `infra/main.tf`: `local.tags.Project = var.brand`. Nothing else. **Do not put brand into `local.resource_name`** — it names the DynamoDB table and the Cognito user pool, both of which carry `prevent_destroy`, so changing the formula hard-fails the existing prod apply and forcing past it changes every `sub` and orphans every saved card. `bucket_name` stays the discriminator, and every other resource name falls out of it correctly with zero diff. — `local.tags.Project = var.brand`, plus a comment on `local.resource_name` recording that `bucket_name` is the brand-and-environment discriminator and that `var.brand` must never enter the name formula. `terraform validate` passes and both edited files are `terraform fmt`-clean. (`infra/apigateway.tf` has pre-existing `fmt` drift, untouched.)
- [x] 5.6 **Applied.** Apply `infra/bootstrap` with admin credentials (local state, one-time). Confirm the plan is **additions only** before applying. — **BLOCKED: needs admin AWS credentials.** The config is ready and verified additions-only offline.
- [x] 5.7 **Done.** Create two more Google OAuth web clients — `office-dev` and `office-prod` — **in the existing (travel) Google Cloud project**, bringing it to four. One client per environment rather than one shared across both brands, so a leaked or rotated secret is contained to one stack, which is the same no-shared-state rule the rest of the isolation follows. Redirect URIs pinned to `https://<cognito_domain_prefix>.auth.us-east-1.amazoncognito.com/oauth2/idpresponse` — i.e. `officelingobingo-dev` and `officelingobingo-prod`.
- [x] 5.8 **Done — workspaces created and variables set.** Create HCP workspaces `officelingobingo-dev` (VCS-driven, auto-apply) and `officelingobingo-prod` (manual apply) in the **existing** `travel-bingo` HCP project — the project name is embedded in the OIDC subject claim, so a new project needs a per-entry field and a rename silently breaks all four trust policies. Set `bucket_name`, `environment`, `brand`, `domain_name`, `hosted_zone_name`, `cognito_domain_prefix`, the Google client id/secret, and `lambda_execution_role_arn` on each. Consider plan-only on office dev initially — both dev workspaces now watch the same repo. — **BLOCKED: needs HCP access.**
- [x] 5.9 **Applies done and confirmed serving.** Both `https://dev.officelingobingo.com` and `https://officelingobingo.com` return HTTP 200 with the office `<title>`, office description, and `favicon-office.svg`. ⚠️ **Unconfirmed: whether the `Project` cost-allocation tag was enabled in Billing.** It is not retroactive and takes ~24h to appear, so it is worth doing now rather than the first time the question is asked. Apply office dev, verify, then apply office prod. Enable the `Project` cost-allocation tag in Billing so "what does the office site cost" stays answerable rather than estimated. — **BLOCKED: follows 5.6-5.8.**

## 6. CI/CD and go live

- [x] 6.1 **Done — proven by working deploys.** Both office environments deploy successfully, which exercises every variable. `VITE_BRAND=travel` on the pre-existing `dev`/`prod` environments is also confirmed: `https://travelbingo.ca` still serves the travel title and `favicon-travel.svg`, so the travel deploy path did not break. Create GitHub Environments `office-dev` and `office-prod` with `AWS_ROLE_ARN`, `S3_BUCKET`, `CLOUDFRONT_DISTRIBUTION_ID`, `LAMBDA_FUNCTION_NAME`, `VITE_COGNITO_DOMAIN`, `VITE_COGNITO_CLIENT_ID`, `VITE_APP_ORIGIN`, and `VITE_BRAND: office`, from the Terraform outputs. Required reviewers on `office-prod`. — **BLOCKED: GitHub console.** Note that `VITE_BRAND` must also be added to the EXISTING `dev` and `prod` environments (`VITE_BRAND=travel`) — `_deploy.yml` passes it now and the build fails without it. — **Six of the eight values do not depend on the apply** and can be set as soon as the environments exist: `AWS_ROLE_ARN` (bootstrap output), `S3_BUCKET` and `LAMBDA_FUNCTION_NAME` (`<bucket_name>` and `<bucket_name>-api`), `VITE_COGNITO_DOMAIN` (`<bucket_name>.auth.us-east-1.amazoncognito.com`), `VITE_APP_ORIGIN`, and `VITE_BRAND=office`. Only `CLOUDFRONT_DISTRIBUTION_ID` and `VITE_COGNITO_CLIENT_ID` must wait for AWS to assign them. ⚠️ **`VITE_APP_ORIGIN` is the custom domain (`https://dev.officelingobingo.com`), NOT the `site_url` output** — `site_url` returns the `*.cloudfront.net` domain, while `cognito.tf` builds its callback URL from `var.domain_name`. Using `site_url` produces a redirect mismatch at sign-in that reads as a Cognito fault rather than a GitHub variable. **Also set `VITE_BRAND=travel` on the existing `dev` and `prod` environments in the same sitting** — `_deploy.yml` passes it now and the travel build fails without it.
- [x] 6.2 Add `.github/workflows/deploy-office-dev.yml` and `deploy-office-prod.yml`, mirroring the existing pair and calling the same two reusable workflows, each with its **own concurrency group** so the brands do not serialize behind one another. Four explicit files rather than a matrix: `needs: backend` waits for an entire matrix, so an office backend failure would block the travel frontend deploy, and a matrix would put both brands behind one production approval.
- [x] 6.3 Update `README.md`, `frontend/README.md`, `infra/README.md`, `infra/bootstrap/README.md`, and `AGENTS.md`: the `VITE_BRAND` local-dev variable, the four environments, the two-brand capture step in the definition of done, and the `npx shadcn add` follow-up (new tokens land in `index.css` and must be relocated into both brand files). — Updated `README.md` (the `VITE_BRAND` local-dev variable and that the `.env.local` values shown are the travel stack's), `infra/README.md` (four workspaces in one table, the `environment`-is-not-a-label warning, per-brand Google projects, per-brand hosted zones), `infra/bootstrap/README.md` (the `environments` map, why the keys are load-bearing, why `github_environment` is explicit), and `AGENTS.md` (per-brand capture and review, build-every-brand in the definition of done, and the `BRAND.md` pointer).
- [x] 6.4 Give `scripts/dev-user.sh` a brand or pool argument — it is pool-specific today. — `BRAND` and `ENV_NAME` now select the stack; both are validated with a clear error. The printed snippets follow: the `curl` host and the `localStorage` key are the selected brand's, with a note that the key is brand-namespaced and that a localhost dev server keys by whichever `VITE_BRAND` it was started with. Verified for all four combinations.

## 7. Verification

- [x] 7.1 `npm run lint && npm test && npm run build` in **both** `frontend/` and `backend/`. Backend must be untouched and still green — that is the check that the "zero backend changes" claim held. — Green in both packages. **`backend/` has zero changes** (`git diff HEAD -- backend/` is empty), which is the check that the no-backend-changes claim held.
- [x] 7.2 Build both brands and confirm isolation: `VITE_BRAND=office npm run build`, then grep `dist/` for a travel-only token value and for "Travel Bingo". `check-bundle.mjs` should already fail on a leak; do it by hand once anyway. — Both brands build; `check-bundle` passes each way. By hand: the office bundle contains no occurrence of "Travel Bingo" and none of the travel palette's warm-cream `oklch(97.5% .012 85)` (it has the cool `oklch(97.5% .004 250)` instead), and it carries the office suggestion content. **This caught a real leak:** `EntryInput.tsx`'s placeholder was a brand-varying string the task list never named, shipping "e.g. See local wildlife" into the office build. It is now `copy.editor.entryPlaceholder`.
- [x] 7.3 Confirm the build fails loudly with `VITE_BRAND` unset and with `VITE_BRAND=nonsense`, and that `npm run dev` still starts with neither set. — Unset: `Error: VITE_BRAND must be one of: travel, office (not set)`. Unrecognized: `... (got "nonsense")`. Both fail the build with the accepted identifiers named. `npm run dev` with neither set starts and serves travel.
- [x] 7.4 Full capture matrix per brand for `/`, `/ui`, `/cards`, `/trips`: light and dark, 390px and 1440px. Confirm the API-request count on load is **zero** for both brands — the signed-out invariant. — 32 captures, 16 per brand, across `/`, `/ui`, `/cards`, `/trips` x light/dark x 390/1440. **No filename collisions** — the brand prefix works. **Zero API requests on load for both brands**, on every route: `capture.mjs` reports a count only when one is made, and it reported none.
- [x] 7.5 Export regression checklist per `DESIGN.md`: print Letter and A4, PNG, thumbnail. The PDFs must be equivalent **across brands** — that is the proof that the frozen renderer is genuinely brand-invariant, and it is the single most important check in this list. — **PASSED, and decisively.** Both brands printed to PDF at Letter and A4; every one of the **30 content streams is byte-identical across the two brands**, in both paper sizes. The only difference anywhere in the files is the `/Title` metadata string (and the byte offsets it shifts). The frozen card renderer is genuinely brand-invariant — this is the proof the whole seam rests on.
- [x] 7.6 Tab through the editor and the trips pages in the office brand. Focus rings are hand-rolled on the colour swatches, the saved-card open button, and the header links, and a new palette is exactly where contrast regressions hide. — **Done by the repo owner at a keyboard.** The office `--ring` is its `--primary`, measured at 5.65:1 on the page; the hand-rolled sites declare `focus-visible:ring-[3px] ring-ring/50`. Static analysis could only establish that the declarations were present — a human confirmed the rings are actually visible against the new palette, which is the part that could not be automated.
- [x] 7.7 Against deployed office dev: sign in with Google end to end (proves the new pool, the new Google client, and the redirect URI). The consent screen will say **"Travel Bingo"** — that is the accepted trade from 0.4, not a bug to chase. Save a card using an office suggested theme (proves the `ALLOWED_FONTS` agreement). Mint a share link and open it. Create a trip and redeem an invite with a second identity from `scripts/dev-user.sh`. — **Done by the repo owner against deployed office dev.** Federated sign-in works end to end, which is the one step no CLI can stand in for: it exercises the new Cognito pool, the Google client, and the redirect URI together. The office suggested theme saved without a 400, confirming the `ALLOWED_FONTS` agreement between `fontScheme.ts` and the backend holds for the office themes.
- [x] 7.8 Confirm the two stacks are genuinely isolated: a card saved on office dev must not appear in travel dev's library, and a travel dev access token must not authenticate against the office API. — **Done, both halves.**
  - **Assets**, against the live office prod bundle: **zero** occurrences of "Travel Bingo", zero travel suggestion/placeholder content, office content present, no dev-gallery sentinel. Travel prod simultaneously still serves its own brand.
  - **Identity.** The same address, `iso-probe@example.com`, was created in *both* dev pools and produced two unrelated identities — different `iss` (`us-east-1_Dw1A5n2io` vs `us-east-1_wVOWNjHuI`), different `client_id`, and different `sub`. Since `sub` is the DynamoDB partition key, this is the isolation claim at its root: there is no address by which one person becomes the same user on both sites.
  - **Data.** A card titled `ISOLATION-PROBE-7C4F` saved on office dev (201, using the office `slide-deck` theme — an independent re-proof of the `ALLOWED_FONTS` agreement, since `'Anton'`/`'Poppins'` had to clear the backend allowlist) was listed by the office token and **absent** from travel dev's library.
  - **Authorization**, tested in *both* directions rather than the one the task names: travel token → office API **401**, office token → travel API **401**. Each token authenticates against its own stack (200) and nothing else. The reverse direction matters as much as the forward one — a shared issuer would have passed a one-way test.
  - Cleaned up: probe card deleted (204, confirmed absent), both Cognito test users deleted, local tokens removed.
- [x] 7.9 Confirm the frozen card renderer and the two contract tests (`cardPayload.contract.test.ts`, `savedCard.contract.test.ts`) were not modified by any part of this change. — `CardGrid.tsx` and both contract tests are **byte-identical to `HEAD`**. `App.css` has exactly one change and it is inside a comment: `see index.css` → `see base.css`, correcting a pointer this change invalidated. No rule, selector, or declaration was touched. The new guard in `cardGrid.guard.test.ts` additionally fails if either file ever references `@/brand` or `brand.`.
