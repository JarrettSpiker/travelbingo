## 1. Backend pure logic

- [x] 1.1 Create `backend/src/lib/playWindow.ts` exporting `isWithinPlayWindow(trip: { startDate?: string; endDate?: string }, now: Date): boolean`. No dates → always open; `startDate` only → no upper bound; `endDate` only → no lower bound; otherwise open from `startDate`T00:00:00Z minus 24h to `endDate`T23:59:59.999Z plus 24h. Parse with `Date.UTC` from the already-validated `^\d{4}-\d{2}-\d{2}$` form — never `new Date(string)`, whose bare-date parsing is UTC but whose datetime parsing is not.
- [x] 1.2 Co-locate `backend/src/lib/playWindow.test.ts`: no dates; start only; end only; both; the instant one millisecond before the window opens and one after it closes; a member on the start date at UTC+14 and on the end date at UTC−12 (the two cases the ±24h margin exists for); `endDate === startDate` (a one-day trip is open for ~72h of wall clock).
- [x] 1.3 Update the table-layout comment at the top of `backend/src/lib/keys.ts` so the `TRIP#<tripId> TRIPCARD#<id>` line records the new `markedSlots` and `progressUpdatedAt` attributes. No key helper changes — progress is a new attribute on an existing item, not a new key prefix.

## 2. Backend authorization

- [x] 2.1 Extend `backend/src/auth.ts` with `requireTripCardPlayer(deps, userId, tripId, tripCardId)`, a third sibling of `requireCardRole`/`requireTripRole`. It calls `requireTripRole(…, ADMIN_OR_MEMBER)`, then Gets the trip `META` and the `TRIPCARD#` item. Missing trip card → `notFound()`. Cooperative → any member passes. Competitive → pass only when `userId === assignedMemberId`; unassigned or a different assignee → `forbidden()`, with no administrator exemption. Return the trip card item and the trip meta so the caller does not re-read them.
- [x] 2.2 Extend `backend/src/auth.test.ts` (or a sibling `auth.tripCard.test.ts`): non-member → 404 from the inherited trip check; member on a missing `tripCardId` → 404; cooperative member → allowed; competitive assignee → allowed; competitive non-assignee → 403; competitive admin who is not the assignee → 403; competitive unassigned → 403 for everyone including the admin. Assert with `fakeDdb`'s `sendCount` that the routine performs exactly three reads.

## 3. Backend progress routes

- [x] 3.1 Add `markTripCardSlot` and `unmarkTripCardSlot` to `backend/src/routes/trips.ts`. Both: `requireTripCardPlayer`; parse `slotIndex` from the path as a whole number in `[0, 25)` (reject anything else with `badRequest`); reject a position whose `snapshot.slots[slotIndex]` is `null` — a blank is not a square; reject when `isWithinPlayWindow(tripMeta, deps.now())` is false. Then a single `UpdateCommand` using `ADD markedSlots :slot` / `DELETE markedSlots :slot` with a `DocumentClient` number set, setting `progressUpdatedAt` in the same expression. Return the card's resulting marks as a sorted JSON array.
- [x] 3.2 Add `getTripProgress` to `backend/src/routes/trips.ts`: `requireTripRole(…, ADMIN_OR_MEMBER)`, one `begins_with(SK, "TRIPCARD#")` Query on the trip partition, projecting only `SK`, `markedSlots`, and `progressUpdatedAt`. Return `[{ tripCardId, markedSlots, progressUpdatedAt }]`. It must not re-serialize snapshots, members, or invites — this is the polled endpoint.
- [x] 3.3 Include `markedSlots` (sorted array, `[]` when the attribute is absent) and `progressUpdatedAt` on each trip card in `getTrip`'s existing response. No extra query: the trip-partition page already returns the attributes. Update `TripCard` in the route's response shape accordingly.
- [x] 3.4 Extend `backend/src/routes/trips.test.ts`: mark and unmark round-trip; idempotent mark and idempotent unmark; unmarking the last square leaves the item with no `markedSlots` attribute and the API still returning `[]`; out-of-range and non-integer `slotIndex` rejected; blank slot rejected; competitive assignee/non-assignee/admin/unassigned matrix; cooperative any-member; out-of-window rejection before start and after end, with reads still succeeding; progress survives reassignment, member removal, and edits or deletion of the original card; removing a trip card and deleting the trip remove the marks; `getTripProgress` returns nothing for a non-member and is a 404, not a 403; two interleaved marks on different squares both persist.
- [x] 3.5 Register the three routes in `backend/src/router.ts`'s `ROUTES` map — `PUT /api/trips/{tripId}/cards/{tripCardId}/marks/{slotIndex}`, `DELETE /api/trips/{tripId}/cards/{tripCardId}/marks/{slotIndex}`, `GET /api/trips/{tripId}/progress` — all authenticated, none `public`.
- [x] 3.6 Run `npm run lint && npm test && npm run build` in `backend/`; all must pass.

## 4. Infra

- [x] 4.1 Add the three route keys to the API Gateway HTTP API configuration in `infra/apigateway.tf`, all carrying the Cognito authorizer. No new Lambda, DynamoDB table, bucket, or Cognito change.
- [x] 4.2 `terraform plan` against dev shows API Gateway route additions only, with no destructive diff to the `prevent_destroy` table or user pool.

## 5. Frontend card renderer (do this before the rest of the frontend, so the export checklist runs early)

- [x] 5.1 Add two optional props to `frontend/src/components/CardGrid.tsx`: `markedSlots?: ReadonlySet<number>` and `onToggleSlot?: (index: number) => void`. Both optional so every existing caller — `CardView`, `SharedCardPage`, `TripDetailPage`, `lib/cardThumbnail.ts` — is unchanged.
- [x] 5.2 In `CardGrid.tsx`, when `onToggleSlot` is present and the cell is not `blank`, give the existing cell `<div>` `role="button"`, `tabIndex={0}`, `aria-pressed`, `onClick`, and an `onKeyDown` handling Enter and Space, plus the class `bingo-cell-playable`. Keep it a `<div>` — no new tag. Keep every `className` a literal string or template literal the guard's regex can read; no `cn(...)` and no variable.
- [x] 5.3 In `CardGrid.tsx`, render `<span className="bingo-mark" aria-hidden="true"><span className="bingo-mark-stroke" /><span className="bingo-mark-stroke" /></span>` inside a cell whose index is in `markedSlots`. Two real spans — not an SVG (breaks the tag allowlist) and not `::before`/`::after` (pseudo-element serialization is the export-regression risk `DESIGN.md` warns about).
- [x] 5.4 Add `.bingo-mark`, `.bingo-mark-stroke`, and `.bingo-cell-playable` rules to the unlayered `frontend/src/App.css`. Absolutely positioned strokes rotated ±45° filling the cell; fixed `rgba(...)` colour — no `var(--color-*)`, no `oklch()`. Tune the alpha so entry text stays readable against both a light and a dark user-chosen `cellColor`. Add `print-color-adjust: exact` for the strokes inside the existing `@media print` block. Give `.bingo-cell-playable:focus-visible` a fixed-colour outline.
- [x] 5.5 Update `frontend/src/components/cardGrid.guard.test.ts`: add `bingo-mark`, `bingo-mark-stroke`, and `bingo-cell-playable` to `ALLOWED_CLASSES`; leave the tag allowlist, the design-token assertions, and the `oklch()` assertion untouched. Add an assertion that the `@media print` block declares `print-color-adjust: exact`, so marks cannot silently stop printing.
- [x] 5.6 Document the marking layer in `frontend/DESIGN.md`: that it is part of the frozen renderer, that its colour is fixed rather than themed, that translucency is a requirement rather than a style choice, and that the export checklist now includes a card carrying marks.
- [x] 5.7 Run the export regression checklist from `frontend/DESIGN.md` against a card with marks and a card without: `npm run capture -- /ui --pdf`, plus a PNG export and a saved-card thumbnail. Confirm an unmarked card is unchanged from before this task group.

## 6. Frontend shared logic and API client

- [x] 6.1 Extract the `toPng` body from `frontend/src/components/CardView.tsx` into `frontend/src/lib/cardPngExport.ts` as `downloadCardPng(node: HTMLElement, title: string)`, keeping `await document.fonts.ready`, `pixelRatio: 2`, and `buildImageFilename` from `lib/imageExport.ts`. Rewrite `CardView.handlePng` to call it, preserving its existing error state.
- [x] 6.2 Create `frontend/src/lib/playWindow.ts` hand-mirroring `backend/src/lib/playWindow.ts`, with a header comment naming the backend file as its counterpart (the convention `lib/tripTypes.ts` documents). Co-locate `playWindow.test.ts` covering the same cases as task 1.2.
- [x] 6.3 Extend `frontend/src/lib/tripTypes.ts`: add `markedSlots: number[]` and `progressUpdatedAt?: string` to `TripCard`, and a `TripCardProgress` interface for the polled endpoint.
- [x] 6.4 Extend `frontend/src/lib/tripApi.ts` with `markTripCardSlot`, `unmarkTripCardSlot`, and `getTripProgress`, using the existing `apiClient` wrapper. Extend `tripApi.test.ts` with the injected-`fetch` pattern already used there.
- [x] 6.5 Create `frontend/src/hooks/useTripProgress.ts`: seeds from the `TripDetail` already loaded, polls `getTripProgress` on a ~10s interval only while `document.visibilityState === "visible"`, re-subscribes on `visibilitychange`, clears its timer on unmount, and exposes an optimistic `toggle(tripCardId, slotIndex)` that rolls back and surfaces an error when the request is refused. Co-locate `useTripProgress.test.ts` covering the pure reducer parts and the pause/resume behaviour with fake timers.

## 7. Frontend trip page

- [x] 7.1 In `frontend/src/pages/TripDetailPage.tsx`, wire each rendered `CardGrid` to `useTripProgress`: pass `markedSlots`, and pass `onToggleSlot` only when the viewer may modify that card — cooperative and a member, or competitive and the assignee — and the trip is inside its play window per `lib/playWindow.ts`.
- [x] 7.2 When a card is visible but not markable, present it as read-only and say why: not your card, unassigned, or outside the trip's dates. Show the play window's state once for the trip rather than repeating it on every card.
- [x] 7.3 Add a per-card export control on `TripDetailPage` calling `downloadCardPng` with that card's `.bingo-card` node (one ref per rendered card) and its snapshot title, available to every member who can see the card. Surface the same failure alert `CardView` uses.
- [x] 7.4 Surface a refused mark — an out-of-window rejection or a lost assignment — as a visible message, with the card reverting to its true state rather than keeping the optimistic mark.
- [x] 7.5 Add gallery entries in `frontend/src/dev/gallery/registry.tsx` for the card's new states: unmarked, partially marked, fully marked, playable (interactive), and read-only-with-reason. `coverage.test.ts` covers new components; these are new *states*, which nothing mechanical catches, so add them deliberately.

## 8. Verification

- [x] 8.1 `npm run lint && npm test && npm run build` pass in **both** `frontend/` and `backend/`.
- [x] 8.2 Visual QA via `npm run capture -- /trips/:tripId --pdf` in light and dark at 390px and 1440px, with a card carrying marks. Confirm the translucent X leaves entry text readable against both a light and a dark user-chosen cell colour.
- [x] 8.3 Run the full export regression checklist in `frontend/DESIGN.md` — print, PNG, thumbnail — for a marked card and an unmarked card, confirming the unmarked output matches pre-change output.
- [x] 8.4 Confirm `npm run capture -- /` reports zero `/api/` requests signed out and shows no play affordance.
- [x] 8.5 Multi-member verification in dev using `scripts/dev-user.sh` for a second identity: in a competitive trip, the assignee marks and the other member and the admin are both refused; the admin reassigns and the right to mark moves while the marks stay; in a cooperative trip both members mark the same card and each sees the other's marks appear without reloading; set the trip's end date to yesterday and confirm marking is refused while reading still works.
- [x] 8.6 Confirm the saved-card contract tests (`backend/src/lib/cardPayload.contract.test.ts`, `frontend/src/lib/savedCard.contract.test.ts`) are unchanged — this change does not touch the stored card shape.
