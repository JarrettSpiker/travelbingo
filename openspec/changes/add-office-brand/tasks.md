## 0. Spikes and prerequisites

Phases 1–4 leave the live travel site visually identical; nothing below is depended on until 0.1 and 0.2 answer.

- [ ] 0.1 **Spike the CSS alias.** Prove that `@import "#brand-theme"` inside `frontend/src/index.css` resolves through `resolve.alias` under `@tailwindcss/vite`, in **both** `vite dev` (with HMR on edit) and `vite build`. Code-reading says `@tailwindcss/vite` builds its `customCssResolver` from Vite's own resolver, but the whole CSS plan rests on this. Throw the spike away. If it fails, fall back to the ~15-line `transform(code, id)` plugin described in design.md and record the fallback in the commit message.
- [ ] 0.2 **Spike dead-code elimination.** Prove that a `define`d `import.meta.env.VITE_BRAND` literal plus a ternary in `src/brand/index.ts` drops the unselected brand module: build with a throwaway second brand containing a unique sentinel string, then `grep dist/assets/*.js` for it. Confirm that a bare `import.meta.env.VITE_BRAND` with the variable **unset** does *not* get substituted — this is why `define` is required and not a stylistic preference.
- [ ] 0.3 Register `officelingobingo.com`, create its Route53 hosted zone in the same AWS account, and point the registrar's NS records at it. Long lead time — start before any code.
- [ ] 0.4 Create a **new** Google Cloud project for the office brand, with its own OAuth consent screen: correct app name, support email, and logo. The consent screen is shown to the user during sign-in, so an Office Lingo Bingo visitor being asked to grant access to "Travel Bingo" is a trust bug no test can catch. Do not reuse the travel project.
- [ ] 0.5 Check whether the live `travelbingo-prod` HCP workspace sets `environment = "prod"` explicitly. If it does not, DynamoDB deletion protection is currently **off in production** — record the finding; task 5.4 fixes it.
- [ ] 0.6 Verify Cognito's current user-pool pricing tier and free-MAU allowance for a pool created today in `us-east-1`. It is the only cost line in this change that could surprise.
- [x] 0.7 Let `add-trips` finish and commit. — **Confirmed: landed in `85e57a7`, `21ec600`, `8c29676`.** Its four trip pages carry ~150 `trip` references, of which the user-facing subset (roughly 40 distinct strings) is what phase 3 retrofits; the rest are identifiers, types, and route paths that stay `trip` in every brand.

## 1. The brand seam, travel only — no visual change

- [ ] 1.1 Create `frontend/src/base.css` and move into it everything brand-independent from `index.css`: `@custom-variant dark`, the whole `@theme inline` bridge, the `html {}` base paint, `.emoji-picker-themed`, the `prefers-reduced-motion` kill switch, and the prose comments that make those decisions readable. Leave `index.css` as **imports only** — `@import "tailwindcss"`, `#brand-theme`, `#brand-motifs`, `./base.css`, in that order. CSS requires `@import` to precede all other rules, so a `:root` block left in `index.css` would sit after the brand import and win; that is why this file ends up empty of rules.
- [ ] 1.2 Create `frontend/src/brand/travel/theme.css` with the `:root` and `[data-theme="dark"]` token blocks, `--radius`, and `--font-display`. Move the `@fontsource-variable/outfit` import out of `main.tsx` and into this file — it is a travel-brand asset, and the office brand will download no chrome font at all.
- [ ] 1.3 Create `frontend/src/brand/travel/motifs.css` with the two `@utility` definitions, renamed by role: `edge-perf` → `panel-edge`, `bg-map-grid` → `bg-page-texture`. Carry the existing comments across, including the one-motif-per-surface warning on the perforated edge.
- [ ] 1.4 Rename `--shadow-postcard` → `--shadow-raised` and `--ocean`/`--color-ocean` → `--brand-accent` across `theme.css`, `base.css`, and every consumer: `AppShell.tsx`, `SiteHeader.tsx`, `CardView.tsx`, `Panel.tsx`, `SuggestionsDialog.tsx`, `ui/chip.tsx`, `SavedCardsPage.tsx`, `SharedCardPage.tsx`, `dev/gallery/designSamples.tsx`. `--paper` and `--stamp` are already role-named and stay. **Do not touch** `src/lib/colorScheme.ts` or `suggestedThemes.json` — they match a grep for "ocean"/"paper" but those are card content (a theme *named* ocean), not tokens.
- [ ] 1.5 Add `vite.config.ts` brand plumbing: the `BRAND_IDS` closed list, `VITE_BRAND` validation that **throws on `vite build`** and defaults to `travel` on `vite dev`, `define: { "import.meta.env.VITE_BRAND": JSON.stringify(brandId) }`, and the `#brand-theme` / `#brand-motifs` aliases. Required-on-build is what stops a misconfigured CI job shipping travel assets to the office bucket; defaulted-on-dev is what preserves "a fresh clone with no `.env.local` still works".
- [ ] 1.6 Create `frontend/src/brand/types.ts` (`BrandId`, `Brand`, `BrandCopy`, `BrandMeta`) and `frontend/src/brand/travel/index.ts`. `Brand` carries `id`, `name`, `storagePrefix`, `MarkIcon`, `copy`, `suggestions`, `meta`.
- [ ] 1.7 Create `frontend/src/brand/index.ts` selecting with a **ternary**, not a `Record` lookup — an object literal referencing both arms defeats dead-code elimination. Create `frontend/src/brand/registry.ts` holding `{ travel } satisfies Record<BrandId, Brand>`, imported **only by tests** so it never enters the production graph.
- [ ] 1.8 Move `src/data/suggestedCells.json` and `suggestedThemes.json` to `src/brand/travel/`; point `src/lib/suggestions.ts` at `brand.suggestions` instead of its two static imports. The normalizer and `appendCells` are untouched — the JSON is the only thing that moves.
- [ ] 1.9 Create `frontend/src/brand/tokens.contract.test.ts`: read `base.css` as text, extract every `var(--x)` referenced by the `@theme inline` bridge, and assert each is declared by every brand in **both** `:root` and `[data-theme="dark"]`; assert every brand's `motifs.css` defines the full slot list; assert neither `index.css` nor `base.css` declares a brand-owned custom property (the `npx shadcn add` drift guard). This is the highest-value new test — a token missing from one brand renders as *nothing*, with no error anywhere.
- [ ] 1.10 Create `frontend/src/brand/brand.contract.test.ts`: registry exhaustive over `BrandId`; `index.ts`'s ternary covers every registry key (source-read, in the style of `dev/gallery/coverage.test.ts`); `copy` key sets match recursively across brands; `storagePrefix` values unique and non-empty; each brand's suggestion data normalizes to ≥1 category and ≥1 theme. That last assertion also closes an existing gap — `normalizeCategory`/`normalizeTheme` have no test today, so malformed JSON degrades silently to an empty dialog.
- [ ] 1.11 Extend `frontend/scripts/check-bundle.mjs` to assert the built bundle contains the selected brand's `name` and **not** any other brand's. It already proves the dev gallery was eliminated; this is the same mechanism applied to the same risk.
- [ ] 1.12 Add one assertion to `frontend/src/components/cardGrid.guard.test.ts`: `CardGrid.tsx` must not contain `@/brand` or `brand.`. One line, closing the single biggest new risk to the frozen renderer — someone reaching for `brand.copy` inside the card.
- [ ] 1.13 Add `VITE_BRAND: travel` to the `dev` and `prod` GitHub Environment `vars`, and `VITE_BRAND: ${{ vars.VITE_BRAND }}` to the build step's `env` in `.github/workflows/_deploy.yml`.
- [ ] 1.14 **Definition of done for this phase: no visual change.** `npm run capture -- /` and `-- /ui` byte-identical against copies taken before the change, plus the full export regression checklist from `DESIGN.md` (print Letter and A4, PNG, thumbnail) — `--shadow-raised` touches `CardView`, so the card's surroundings changed even though the card did not.

## 2. Marketing surface, travel only

- [ ] 2.1 Create `frontend/src/brand/travel/meta.json`: `name`, `title`, `description`, `themeColorLight`, `themeColorDark`, `markHex`, `markFgHex`, `faviconPath`. **JSON, not TS** — `vite.config.ts` runs in Node and must read the same data the app reads, and `brand/<id>/index.ts` imports `lucide-react`, which would drag React into config load.
- [ ] 2.2 Add a local `brandHtml()` `transformIndexHtml` plugin to `vite.config.ts`: replace `<title>`, rewrite the icon `href`, and inject `description`, `og:title`/`og:description`/`og:type`/`og:url`, `twitter:card`/`twitter:title`/`twitter:description`, `<link rel="canonical">`, and two `theme-color` metas (one under `media="(prefers-color-scheme: dark)"`). `og:url` and `canonical` come from the existing `VITE_APP_ORIGIN`; **omit both when it is unset** rather than emitting an unresolvable URL. Omit `og:image` entirely until a preview image exists.
- [ ] 2.3 Rename `public/favicon.svg` → `public/favicon-travel.svg`.
- [ ] 2.4 Create `frontend/src/brand/meta.contract.test.ts`: each brand's favicon file exists and contains both `markHex` and `markFgHex`; `title` and `description` non-empty; description under ~155 characters. This converts the existing hand-sync comment in the favicon into a test — it cannot verify the hex matches the oklch without a colour library, but it catches the palette moving without the favicon following, which is the failure that actually happens.
- [ ] 2.5 Extend `check-bundle.mjs` to read `dist/index.html` and assert its title, description, and favicon path match the selected brand's `meta.json`.
- [ ] 2.6 Verify: the travel site gains a description, OG, Twitter, canonical, and theme-color tags it does not have today. Confirm the rendered `<head>` in `dist/index.html` and that the favicon still resolves.

## 3. Copy extraction, travel only

- [ ] 3.1 Define `BrandCopy` in `brand/types.ts` and populate `brand/travel/copy.ts`. Extract the ~25 varying strings: the wordmark in `SiteHeader.tsx`, the nav labels, `CardDetailsForm.tsx`'s title placeholder, `SharedCardPage.tsx`'s fallback card name, and `imageExport.ts`'s `DEFAULT_IMAGE_FILENAME`. Capitalized variants are **explicit keys**, not a `capitalize()` helper.
- [ ] 3.2 Retrofit the Trips UI: move the ~40 user-facing "trip" occurrences in `TripsPage.tsx`, `TripFormPage.tsx`, `TripDetailPage.tsx`, `InvitePage.tsx`, and the `SiteHeader` nav entry to `brand.copy.trips.*` / `brand.copy.noun.*`, and make the `Compass` icon brand-selected. Component names, file names, `tripApi.ts`, `tripTypes.ts`, and the `TRIP#` key prefix all stay `trip` in every brand.
- [ ] 3.3 Create `frontend/src/lib/routes.ts` (`ROUTES = { editor, cards, trips, settings, … } as const`) and use it at every `NavLink to` / `navigate()` / `signIn()` / `<Navigate to>` site. Paths stay brand-invariant — invite and share links are persisted capability URLs — but this makes the decision reversible in one file rather than twenty call sites.
- [ ] 3.4 Route `authSession.ts` and `colorMode.ts` storage keys through `brand.storagePrefix`. Not urgent (the two sites are different origins, so localStorage is already isolated) but it removes a confusing hardcoded string for three lines.
- [ ] 3.5 Add to `AGENTS.md` the rule that keeps `BrandCopy` from metastasizing: **a string enters it only when the two brands' values actually differ.** `Save`, `Cancel`, `PDF`, `PNG`, `Entries`, `Look & feel`, `Card details` are identical in both brands and stay inline.
- [ ] 3.6 Verify: `npm run capture` still identical to the pre-change copies. This phase moves strings; it must not move pixels.

## 4. The office brand — frontend only, nothing deployed

- [ ] 4.1 `brand/office/theme.css`: cool printer-paper off-white page (not warm cream), flat corporate blue `--primary`, highlighter yellow-green `--brand-accent`, bureaucratic red `--stamp`, cool slate dark mode (not warm navy), squarer `--radius`, and `--font-display` set to the system stack so **no chrome font is downloaded** — cheaper, and exactly the joke.
- [ ] 4.2 `brand/office/motifs.css`: `bg-page-texture` as spreadsheet cell ruling (the same 64px grid SVG with the tick paths deleted); `panel-edge` as a three-hole-punch binder edge (same `radial-gradient` mask technique, three holes down the left, `mask-repeat: no-repeat`).
- [ ] 4.3 **Check the new contrast hazard by hand.** A blue `--primary` retires the terracotta/crimson confusion `DESIGN.md` warns about and creates a new one: red `--stamp` against `--destructive`. The token guard checks presence, never value, so this is a human check — both presentations, and the chip and alert surfaces specifically.
- [ ] 4.4 `brand/office/copy.ts` and `meta.json`; `public/favicon-office.svg` with the `TrendingUp` mark; `MarkIcon: TrendingUp` in `brand/office/index.ts`. Add `office` to `BRAND_IDS`, to the registry, and to the ternary in `brand/index.ts` — the guards from 1.9/1.10 now have a second brand to compare against and start earning their keep.
- [ ] 4.5 `brand/office/suggestedCells.json`: roughly four categories × 15 cells. Proposed categories — Standup, All-Hands, Client Call, Performance Review. Proposed cells — "You're on mute", "Let's take that offline", "Circle back", "Can everyone see my screen?", "Let's parking-lot that", "Low-hanging fruit", "Double-click on that", "Do you have bandwidth?", "Move the needle", "Boil the ocean", "Table stakes", "North star", "Deep dive", "Socialize this", "Ping me". First draft; expected to be revised in 4.8.
- [ ] 4.6 `brand/office/suggestedThemes.json`: Slide Deck, Spreadsheet, Legal Pad, Whiteboard, Redline, Q4 Earnings. These carry raw hex, which is correct — they are card content, not app chrome. **Every font must come from the shared `ALLOWED_FONTS` allowlist**, which `cardPayload.ts` duplicates byte-for-byte from `fontScheme.ts`; a theme naming anything else makes the card unsaveable with a 400 far from the cause.
- [ ] 4.7 Extend `brand.contract.test.ts` with the font check: every font in every brand's `suggestedThemes.json` is in `ALLOWED_FONTS`.
- [ ] 4.8 Add `VITE_BRAND` to `frontend/scripts/capture.mjs`'s output slug. Without it, capturing `/` in the office brand **silently overwrites** the travel captures — a genuinely confusing review failure. Then review `/`, `/ui`, `/cards`, and `/trips` in both brands, light and dark, 390px and 1440px, and iterate on the office palette and content. **This is where the design is judged, with zero infrastructure committed and zero cost incurred.**
- [ ] 4.9 Write `frontend/src/brand/travel/BRAND.md` and `office/BRAND.md` with each brand's palette, typeface, and motif realizations. Rewrite `frontend/DESIGN.md` to hold the **rules** only — token roles, motif slots, the one-motif-per-surface constraint, spacing, type, depth, focus, the frozen renderer, the export checklist — pointing at the BRAND.md files for specifics. Do not turn DESIGN.md into two columns; its value is that it is specific, and parameterizing it would make both halves mushy.
- [ ] 4.10 Land `.github/workflows/ci.yml` from `openspec/changes/add-branch-protection/`, with a build matrix over both `VITE_BRAND` values. **This is a prerequisite, not polish**: lint/test/build currently run only on the deploy path, and a build exercises only the selected brand's CSS graph — so without it, nothing would ever build the office brand except a production deploy.

## 5. Infrastructure

- [ ] 5.1 `infra/bootstrap`: replace the four scalar `dev_*`/`prod_*` variables with one `variable "environments"` of type `map(object({ bucket, workspace, role_name_prefix, github_environment }))`, and turn `local.envs` into a comprehension over it in `tfc-roles.tf`, `gha-roles.tf`, and `lambda-roles.tf`.
- [ ] 5.2 Keep the existing map keys **`dev` and `prod`** and set their `role_name_prefix` to `travelbingo`, so `terraform plan` shows additions only. `for_each` keys are part of the resource address (`aws_iam_role.tfc["dev"]`), and the existing role ARNs are pasted into HCP and GitHub configuration — a destroy/recreate breaks the running deploys. New keys: `office-dev`, `office-prod`, prefix `olbingo`.
- [ ] 5.3 Make `github_environment` an explicit field rather than reusing `each.key`. Today `gha-roles.tf` builds the OIDC subject from `each.key` while `tfc-roles.tf` builds role names from `role_suffix`; they are equal **by coincidence**, and a new entry where they differ would silently produce a role nothing can assume. Update the `table_name`/`function_name` comment to say that `bucket_name` is the brand-and-environment discriminator.
- [ ] 5.4 `infra/variables.tf`: add `variable "brand" { default = "travelbingo" }`; change `environment`'s default from `"production"` to `"prod"` and add `validation { contains(["dev","prod"], var.environment) }`; correct its description, which says "used for tagging" when it is actually a behaviour switch gating the Cognito admin-auth flow, the localhost callback URL, and DynamoDB deletion protection. Act on the 0.5 finding.
- [ ] 5.5 `infra/main.tf`: `local.tags.Project = var.brand`. Nothing else. **Do not put brand into `local.resource_name`** — it names the DynamoDB table and the Cognito user pool, both of which carry `prevent_destroy`, so changing the formula hard-fails the existing prod apply and forcing past it changes every `sub` and orphans every saved card. `bucket_name` stays the discriminator, and every other resource name falls out of it correctly with zero diff.
- [ ] 5.6 Apply `infra/bootstrap` with admin credentials (local state, one-time). Confirm the plan is **additions only** before applying.
- [ ] 5.7 Create the two Google OAuth web clients in the 0.4 project, redirect URIs pinned to `https://<cognito_domain_prefix>.auth.us-east-1.amazoncognito.com/oauth2/idpresponse`.
- [ ] 5.8 Create HCP workspaces `officelingobingo-dev` (VCS-driven, auto-apply) and `officelingobingo-prod` (manual apply) in the **existing** `travel-bingo` HCP project — the project name is embedded in the OIDC subject claim, so a new project needs a per-entry field and a rename silently breaks all four trust policies. Set `bucket_name`, `environment`, `brand`, `domain_name`, `hosted_zone_name`, `cognito_domain_prefix`, the Google client id/secret, and `lambda_execution_role_arn` on each. Consider plan-only on office dev initially — both dev workspaces now watch the same repo.
- [ ] 5.9 Apply office dev, verify, then apply office prod. Enable the `Project` cost-allocation tag in Billing so "what does the office site cost" stays answerable rather than estimated.

## 6. CI/CD and go live

- [ ] 6.1 Create GitHub Environments `office-dev` and `office-prod` with `AWS_ROLE_ARN`, `S3_BUCKET`, `CLOUDFRONT_DISTRIBUTION_ID`, `LAMBDA_FUNCTION_NAME`, `VITE_COGNITO_DOMAIN`, `VITE_COGNITO_CLIENT_ID`, `VITE_APP_ORIGIN`, and `VITE_BRAND: office`, from the Terraform outputs. Required reviewers on `office-prod`.
- [ ] 6.2 Add `.github/workflows/deploy-office-dev.yml` and `deploy-office-prod.yml`, mirroring the existing pair and calling the same two reusable workflows, each with its **own concurrency group** so the brands do not serialize behind one another. Four explicit files rather than a matrix: `needs: backend` waits for an entire matrix, so an office backend failure would block the travel frontend deploy, and a matrix would put both brands behind one production approval.
- [ ] 6.3 Update `README.md`, `frontend/README.md`, `infra/README.md`, `infra/bootstrap/README.md`, and `AGENTS.md`: the `VITE_BRAND` local-dev variable, the four environments, the two-brand capture step in the definition of done, and the `npx shadcn add` follow-up (new tokens land in `index.css` and must be relocated into both brand files).
- [ ] 6.4 Give `scripts/dev-user.sh` a brand or pool argument — it is pool-specific today.

## 7. Verification

- [ ] 7.1 `npm run lint && npm test && npm run build` in **both** `frontend/` and `backend/`. Backend must be untouched and still green — that is the check that the "zero backend changes" claim held.
- [ ] 7.2 Build both brands and confirm isolation: `VITE_BRAND=office npm run build`, then grep `dist/` for a travel-only token value and for "Travel Bingo". `check-bundle.mjs` should already fail on a leak; do it by hand once anyway.
- [ ] 7.3 Confirm the build fails loudly with `VITE_BRAND` unset and with `VITE_BRAND=nonsense`, and that `npm run dev` still starts with neither set.
- [ ] 7.4 Full capture matrix per brand for `/`, `/ui`, `/cards`, `/trips`: light and dark, 390px and 1440px. Confirm the API-request count on load is **zero** for both brands — the signed-out invariant.
- [ ] 7.5 Export regression checklist per `DESIGN.md`: print Letter and A4, PNG, thumbnail. The PDFs must be equivalent **across brands** — that is the proof that the frozen renderer is genuinely brand-invariant, and it is the single most important check in this list.
- [ ] 7.6 Tab through the editor and the trips pages in the office brand. Focus rings are hand-rolled on the colour swatches, the saved-card open button, and the header links, and a new palette is exactly where contrast regressions hide.
- [ ] 7.7 Against deployed office dev: sign in with Google end to end (proves the new pool, the new Google client, and the redirect URI), and confirm the consent screen shows the **Office Lingo Bingo** app name. Save a card using an office suggested theme (proves the `ALLOWED_FONTS` agreement). Mint a share link and open it. Create a trip and redeem an invite with a second identity from `scripts/dev-user.sh`.
- [ ] 7.8 Confirm the two stacks are genuinely isolated: a card saved on office dev must not appear in travel dev's library, and a travel dev access token must not authenticate against the office API.
- [ ] 7.9 Confirm the frozen card renderer and the two contract tests (`cardPayload.contract.test.ts`, `savedCard.contract.test.ts`) were not modified by any part of this change.
